- if @team_request
  %p You requested to join #{@team_request.team.name}. An email has been sent to their captain. If your request is accepted, you will be added to their roster!
- else
  %ul
    %li= link_to "Create Team", new_team_path
    %li= link_to "Join Team", teams_join_path
